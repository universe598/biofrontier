# 参与贡献

欢迎通过 Issue 提交使用体验、来源适配建议、分类体系问题和可复现的错误，也欢迎提交 Pull Request。

## 开发流程

1. 使用 Node.js 22 或更高版本；
2. 运行 `npm ci` 安装锁定依赖；
3. 修改应保持本地优先、手动触发外部 AI 和不绕过访问权限的产品边界；
4. 提交前运行 `npx tsc --noEmit`、`npm run build` 和 `node tests/smoke.cjs`；
5. 涉及本地模型时，说明模型版本、量化方式、显存占用和是否完成真实模型测试。

请不要提交真实 API Key、个人数据库、下载的论文、GGUF 模型、推理运行时、`node_modules` 或构建产物。

分类与专题报告属于辅助功能。涉及提示词、分类体系或报告结构的修改，请同时说明误判风险和人工核验方式。
