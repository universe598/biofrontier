# 安全说明

## 报告安全问题

请不要在公开 Issue 中粘贴 API Key、私人论文、数据库或其他敏感材料。发现可能泄露密钥、执行任意代码、绕过本地边界或访问非预期文件的问题时，请通过仓库维护者提供的私密联系方式报告；在仓库尚未配置私密联系方式前，可以只在 Issue 中说明“需要私下报告安全问题”。

## 安全边界

- Renderer 启用 Electron sandbox 与 context isolation；
- 外部网页不会在应用窗口中直接运行，而是交给系统浏览器；
- 自定义 RSS、网页和下载默认拒绝本机与局域网地址，并逐次检查重定向目标；
- API Key 在主进程中读取和使用，不暴露为网页脚本可直接访问的设置值；
- 外部 AI 正文请求要求用户先确认对应服务商的数据发送提示；
- 本地 llama.cpp 服务只绑定本机地址，并为每次启动生成随机访问密钥；
- 软件不会尝试绕过付费墙、登录、验证码或机构权限。

## 发布前检查

- 搜索并移除真实 API Key、`.env`、数据库、下载资料和个人路径；
- 不提交或捆绑 `node_modules`、构建目录、GGUF 模型、llama.cpp 或 CUDA 二进制；
- 对 Release 文件计算并公布 SHA-256；
- 使用 `npm ci`、TypeScript 检查和 smoke test 验证发布提交；
- 依赖更新后重新检查 Electron 安全设置和外部请求代码。

当前个人构建没有 Windows 代码签名证书，SmartScreen 可能显示未知发布者。下载者应只从项目的正式 Release 页面取得文件，并核对 SHA-256。
