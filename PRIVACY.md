# 隐私说明

版本：2026-09-04-v2

BioFrontier 是本地优先的桌面软件，不提供用户账户，也没有自建遥测、广告或分析服务。

## 保存在本机的数据

- 昵称、个人说明和头像；
- 已同步文章、阅读状态、收藏、分类结果和 AI 专题报告；
- 下载的公开 PDF 与网页副本；
- 用户配置的内容来源、关注方向、本地模型与运行环境路径；
- 外部 AI API Key，以及用户是否已确认向对应服务商发送数据。

API Key 使用 Electron `safeStorage` 调用 Windows 系统安全能力加密。其他数据保存在应用数据目录或用户选择的资料库目录。

## 会发生的联网请求

- 同步用户启用的 PubMed/Europe PMC、bioRxiv、medRxiv、arXiv 或 RSS/Atom 来源；
- 打开原始论文网页、下载可公开取得的 PDF 或保存用户指定的公开网页；
- 用户手动调用 DeepSeek、OpenAI 或 Anthropic Claude 进行初读、需求匹配、文章分类或专题速报时，把该次任务需要的标题、摘要、来源分类、关注方向或专题问题发送给所选服务商；
- 用户手动读取外部 AI 模型列表时，向所选服务商验证 Key 并取得模型信息；
- 用户点击本地 AI 官方下载按钮时，在外部浏览器打开 Hugging Face 或 GitHub 官方页面。

软件不会在同步后自动把内容发送给外部 AI。API 分类和 API 专题速报也只在用户点击开始后运行。首次向每个服务商发送正文数据前，软件会显示数据发送提示并取得确认；用户可在设置中让该提示再次出现。服务商收到的数据及产生的费用受其自身条款、隐私政策和计费规则约束。

用户可以仅同意一次，也可以选择记住对某个服务商的同意。仅本次授权只保存在运行内存中，并在该次请求开始时消耗；“以后不再弹出此确认框”的选择会保存在本机，直至用户在设置中重置。

选择本地模型时，分类和专题报告生成不会把文章内容发送给外部 AI 服务。无论选择本地模型还是外部 API，专题搜集本身仍需访问用户启用的公开文献来源。

## 自定义来源安全

为减少自定义网址被用于访问设备内部服务的风险，软件默认拒绝本机、环回、链路本地和常见私有网络地址，并对 HTTP 重定向后的地址再次检查。确需使用可信内网 RSS 或网页时，用户可以在高级设置中主动允许；此时访问风险由用户控制。

## 用户控制

用户可以关闭任一内容来源、删除自定义订阅、移除某个外部 API Key、重置数据发送确认、停止或清除本地模型配置，并通过删除应用数据和资料库文件清除本地记录。卸载程序不会主动删除用户另行选择的资料库目录。
